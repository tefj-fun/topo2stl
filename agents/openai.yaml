interface:
  display_name: "Topology Print Release"
  short_description: "Validate BESO designs through printable release"
  default_prompt: "Use $topology-print-release to build or audit this topology-optimized printable part."
